import { Routes } from '@angular/router';
import { PageIndexComponent } from './page-index/page-index.component';

export const routes: Routes = [
    {path: '', component: PageIndexComponent, title: 'Naruenat.K'}
];
